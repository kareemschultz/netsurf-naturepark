{
  "name": "api",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "start": "bun src/index.ts",
    "build": "echo 'API runs directly on Bun — no build step'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.3",
    "@workspace/db": "workspace:*",
    "@workspace/shared": "workspace:*",
    "drizzle-orm": "^0.43.1",
    "hono": "^4.8.3",
    "postgres": "^3.4.5",
    "zod": "^3.25.64"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.3"
  }
}
